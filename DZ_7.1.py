def say_hi(name: str, age: int) -> str:
    """
    Returns a greeting string with name and age.

    :param name: Person's name
    :param age: Person's age
    :return: Greeting string
    """
    return f"Hi. My name is {name} and I'm {age} years old"
